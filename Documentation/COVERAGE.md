# Code Coverage

## Scope

The coverage target is the production assembly `KUKULCAN.SharedKernel.Database`.
Test assemblies are excluded from the production coverage measurement.

Coverage is evaluated from two complementary sources:

- **Deterministic unit tests** provide line and branch coverage of the production library.
- **Provider-specific integration tests** validate the same infrastructure against each supported relational DBMS.

The supported providers are:

- Microsoft SQL Server
- PostgreSQL
- MySQL

## Latest Aggregate Coverage

The latest aggregate report generated by `Coverage.sh` combines the coverage results produced by the complete solution test run, including the deterministic test suite and the provider-specific integration suites.

The measured baseline from the current `main` commit is:

| Metric | Result |
|---|---:|
| Line coverage | **96.96% (383/395)** |
| Branch coverage | **91.14% (144/158)** |

This is the latest measured aggregate coverage supplied by the repository coverage report.

The report contains **12 uncovered production lines** and **14 uncovered branches**. The remaining gaps are concentrated in `KukulcanDbContextBase` and a small number of constructor decision points in other production classes.

## Production-Class Coverage

The most relevant class-level results from the latest report are:

| Production class | Line coverage | Branch coverage |
|---|---:|---:|
| `KukulcanDbContextBase` | **93.58%** | **85.53%** |
| `DomainEventDispatchInterceptor` | **100%** | **83.33%** |
| `KukulcanDatabaseStartupInitializer<TContext>` | **100%** | **85.71%** |
| `AuditSaveChangesInterceptor` | **100%** | **100%** |
| `ImmutableEntityInterceptor` | **100%** | **100%** |
| `SlowQueryInterceptor` | **100%** | **100%** |
| `SoftDeleteInterceptor` | **100%** | **100%** |
| `TenantModelCacheKeyFactory` | **100%** | **100%** |
| `UnitOfWork<TContext>` | **100%** | **100%** |

The primary remaining production-code coverage area is `KukulcanDbContextBase`.

## `KukulcanDbContextBase` Coverage Focus

The latest report identifies the following executable paths as the main remaining coverage gaps:

- Empty or whitespace connection strings in `AppendConnectionStringOptions` and `RemoveConnectionStringKeys`.
- Defensive provider configuration failure handling in `ConfigureSqlServer`.
- Provider extension type discovery when `Assembly.GetType(...)` cannot resolve the expected fully qualified name.
- Provider extension discovery through `Assembly.GetTypes()`.
- Provider assemblies that partially fail type loading and therefore raise `ReflectionTypeLoadException`.
- The final unsupported-provider-extension path when the expected type cannot be found.

These paths represent real defensive behavior in the provider-loading and connection-string infrastructure. They are therefore valid coverage targets and are tested explicitly in the `TEST/Coverage` branch.

## Provider Extension Type Resolution

`KukulcanDbContextBase` resolves provider extension types dynamically because the consuming application is responsible for providing the corresponding EF Core provider package.

The resolution logic has three meaningful paths:

1. Resolve the expected fully qualified type directly with `Assembly.GetType(...)`.
2. Fall back to `Assembly.GetTypes()` and match the expected short type name.
3. Handle `ReflectionTypeLoadException` and inspect the loadable types returned by `ReflectionTypeLoadException.Types`.

The test suite covers these paths, including a partially loadable provider fixture with a deliberately missing dependency.

The normal provider configuration path remains unchanged: SQL Server, PostgreSQL and MySQL continue to resolve their provider extension types by the configured assembly and expected type name.

## Connection-String Edge Cases

The connection-string helpers contain explicit defensive handling for empty or whitespace-only connection strings.

The test suite now verifies that:

- `AppendConnectionStringOptions` returns the supplied options unchanged when the connection string is empty.
- `RemoveConnectionStringKeys` returns the empty connection string unchanged when there is nothing to process.

These tests cover actual supported defensive behavior rather than introducing artificial execution paths solely for coverage purposes.

## ConfigureSqlServer Coverage Investigation

`ConfigureSqlServer` already has a dedicated test that verifies provider configuration failures are wrapped in the expected `NotSupportedException`.

The latest Cobertura report nevertheless showed the `NotInstalled(...)` throw line as uncovered. This discrepancy is being treated as a coverage/instrumentation issue until a fresh execution confirms whether the current test reaches the mapped sequence point.

No change has been made to the `ConfigureSqlServer` test merely to increase the coverage percentage.

## Integration-Test Coverage

Integration coverage must be measured independently for each real DBMS. The provider-specific integration workflows execute against real database engines and should be considered the authoritative source for provider-backed execution coverage.

The aggregate `Coverage.sh` result must not be interpreted as proof that every production line is independently exercised by every database provider. A line executed by one provider is covered in the aggregate report even when another provider does not execute that same path.

| DBMS | Integration coverage |
|---|---:|
| Microsoft SQL Server | **Measured by the SQL Server integration workflow** |
| PostgreSQL | **Measured by the PostgreSQL integration workflow** |
| MySQL | **Measured by the MySQL integration workflow** |

Provider-specific numerical percentages are intentionally kept separate from the aggregate unit/integration measurement unless the corresponding provider coverage artifact has been explicitly published.

## Integration-Test Scope

The provider suites cover the following responsibilities:

| Responsibility | SQL Server | PostgreSQL | MySQL |
|---|:---:|:---:|:---:|
| Real provider selection and persistence | Yes | Yes | Yes |
| Tenant filtering/isolation | Yes | Yes | Yes |
| Tenant model-cache isolation | Yes | Yes | Yes |
| Audit interceptor | Yes | Yes | Yes |
| Soft delete | Yes | Yes | Yes |
| Domain-event dispatch | Yes | Yes | Yes |
| Immutable entity protection | Yes | Yes | Yes |
| Slow-query diagnostics | Yes | Yes | Yes |
| Cancellation behavior | Yes | Yes | Yes |
| Unit of Work transactions | Yes | Yes | Yes |
| Synchronous persistence paths | Yes | Yes | Yes |
| Asynchronous persistence paths | Yes | Yes | Yes |
| Provider-specific configuration | Yes | Yes | Yes |
| Real retry behavior | Yes | Yes | Yes |
| Real transaction commit/rollback/end semantics | Yes | Yes | Yes |
| Real migration/seed pipeline | Yes | Yes | Yes |

Tests are duplicated between providers only where provider-specific execution can change the result. Provider-independent behavior remains primarily covered by the deterministic unit suite.

## Test Adequacy Decision

The current test strategy is based on meaningful behavior rather than on manufacturing a coverage percentage.

The combination of deterministic unit tests and real-provider integration tests is the intended coverage model for this project.

The remaining coverage work is limited to real defensive and provider-resolution paths. Tests should not be added solely to force a 100% line or branch percentage when a path has no meaningful supported behavior.

## CI Coverage Artifacts

The unit `Code Coverage` workflow generates a Cobertura coverage artifact for the production assembly.

Provider-specific integration workflows generate their own test and coverage artifacts. Those results should be evaluated separately by provider rather than being treated as interchangeable with the aggregate `Coverage.sh` measurement.

## Reproducing the Measurement

From the repository root, run:

```bash
./Coverage.sh
```

The script runs the solution tests, collects Cobertura reports, merges the generated coverage files, and produces the aggregate report used for the figures documented above.

After changes to the coverage suite, the aggregate percentages in this document should only be changed after a fresh `Coverage.sh` execution has produced a new Cobertura report.
