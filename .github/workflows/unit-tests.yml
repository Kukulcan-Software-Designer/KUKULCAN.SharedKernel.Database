name: Database Unit Tests

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  domain-unit-tests:
    name: Build and test Database.UnitTests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore Tests/KUKULCAN.SharedKernel.Database.Tests/KUKULCAN.SharedKernel.Database.Tests.csproj

      - name: Build
        run: dotnet build Tests/KUKULCAN.SharedKernel.Database.Tests/KUKULCAN.SharedKernel.Database.Tests.csproj --no-restore --configuration Release

      - name: Test
        run: dotnet test Tests/KUKULCAN.SharedKernel.Database.Tests/KUKULCAN.SharedKernel.Database.Tests.csproj --no-build --configuration Release --logger "console;verbosity=normal"
